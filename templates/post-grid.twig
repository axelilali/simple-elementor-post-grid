<div class="row">
{% for post in posts %}
	<div class="col-md-{{settings.columns}}">
		<img src="{{post.thumbnail}}" alt="" srcset="">
		<h3>{{post.title}}</h3>
		<p>{{post.content|raw}}</p>
	</div>
{% endfor %}
</div>